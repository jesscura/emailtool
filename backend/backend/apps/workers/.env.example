# General Settings
NODE_ENV=development

# Database Configuration (PostgreSQL/Prisma)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ssemail?schema=public"

# Redis Configuration (BullMQ/Cache)
REDIS_HOST=localhost
REDIS_PORT=6379

# Worker Queue Configuration
# The workers need to know where the API is running for certain callback/status updates
API_BASE_URL=http://localhost:3000

# Email Sending Service Credentials
# Used by the 'backend/packages/email' package
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx